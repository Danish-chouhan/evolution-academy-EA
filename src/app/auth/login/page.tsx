import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login | Evolution Academy',
  description: 'Apna Evolution Academy account se login karo',
};

export default function LoginPage() {
  return <LoginForm />;
}
